[tool.poetry]
name = "autochthon"
version = "0.1.0"
description = "a roguelike dungeon crawler"
authors = ["scott <dev@cracklebones.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
